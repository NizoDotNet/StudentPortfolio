@page
@model Portfolio.Pages.SubjectManager.LabWorksManager.IndexModel
@{
}

@foreach (var lab in Model.LabWorkVM)
{
    <table>
        <tr>
            <td>Name</td>
        </tr>
        <tr>
            <td><a asp-page="LabWork" asp-route-id="@lab.Id">@lab.Name</a></td>
        </tr>
    </table>
}